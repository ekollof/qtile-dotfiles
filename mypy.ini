[mypy]
# Qtile's configuration is checked strictly; optional third-party libraries
# without type metadata should not make `qtile check` fail.
ignore_missing_imports = True

[mypy-qtile_extras.*]
# qtile_extras is a vendored third-party fork with its own upstream typing
# assumptions. Runtime compatibility is verified by importing the fork.
ignore_errors = True
